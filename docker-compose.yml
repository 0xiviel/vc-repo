version: '3'
services:
  app:
    build: ./backend
    #command: bash -c "cd app && alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port 8000"
    command: bash -c "alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port 8000"
    ports:
      - "8000:8000"